cd aimi\apps\api
# If a venv is active: deactivate
Remove-Item -Recurse -Force .venv -ErrorAction SilentlyContinue
python -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install --upgrade pip
pip install -r requirements.txt
uvicorn app.main:app --reload  # http://localhost:8000/v1/health

Keep the API running (that window with Uvicorn is fine).





Health/docs live at:

http://127.0.0.1:8000/v1/health
 (JSON only)

http://127.0.0.1:8000/docs
 (Swagger UI)

Start the frontend (React app) in a new terminal:

cd C:\Users\Admin\OneDrive\Desktop\aimi\apps\web
copy .env.example .env   # (or: cp .env.example .env)
npm install
npm run dev